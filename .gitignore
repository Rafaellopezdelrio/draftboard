# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri Rust build artifacts
src-tauri/target/
src-tauri/Cargo.lock

# Local secrets / app data
*.db
*.db-shm
*.db-wal

# Tauri signing keys — never commit. Private goes in ~/.tauri/, only the
# .pub gets shared. We exclude both as belt-and-braces.
*.key
*.key.pub
.tauri/
.env
.env.local
.env.*.local

# Cloudflare worker node_modules (has its own repo)
cloudflare-worker/node_modules/
cloudflare-worker/.wrangler/

# Vite cache
.vite/

# Test coverage reports
coverage/

# OS
Thumbs.db

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/
